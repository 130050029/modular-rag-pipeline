fastapi
uvicorn[standard]
python-multipart
sentence-transformers
faiss-cpu
numpy
requests
datasets
datasketch
beautifulsoup4
pymupdf
pytest
pytest-xdist
httpx2

# Only needed if config.DB_BACKEND is set to "postgres" -- not required for
# the default SQLite backend. Uses psycopg3 (package name "psycopg"), not
# the older psycopg2.
psycopg[binary]

# Only needed if config.NEAR_DUP_BACKEND is set to "redis" -- not required
# for the default in-memory backend.
redis

# Only needed if config.VECTOR_BACKEND is set to "qdrant" -- not required
# for the default in-process FAISS backend. Lightweight pure-Python client,
# unlike docling below.
qdrant-client

# Only needed if config.PDF_EXTRACTION_METHOD is set to "docling" -- not
# required for the default "pymupdf" method. Heavy (pulls in torch and
# several ML models for layout detection, table structure recognition,
# and OCR) -- installing and running it will be noticeably slower than
# the rest of this file.
docling