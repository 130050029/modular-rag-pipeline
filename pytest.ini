[pytest]
testpaths =
    tests/python

# This ensures rag_python is added to the path even if conftest.py is skipped
pythonpath = rag_python

markers =
    unit: fast isolated tests
    integration: tests involving external services or multiple real components
    e2e: full application/API tests

filterwarnings =
    ignore::DeprecationWarning:docling